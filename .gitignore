# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Tooling / agent output
.playwright-mcp
.impeccable
.prototools

# TinaCMS generated output (also covered by tina/.gitignore)
tina/__generated__

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Output of 'npm pack'
*.tgz

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Yarn Integrity file
.yarn-integrity

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Astro
.astro

# TODO: After scaffolding from this template, DELETE the block below so your
# chosen lockfile (we recommend pnpm-lock.yaml) is committed. See README → "Commit your lockfile".
# Package Manager Lock Files
package-lock.json
pnpm-lock.yaml
yarn.lock
.yarn/cache
bun.lockb
deno.lock

# builds
.vercel
dist

# P0 spike: parked Next.js source for reference; delete at cutover
_legacy-next/
.DS_Store

# Reference sprite sheets. Study only - provenance is not ours to redistribute.
# eagle-astonishia.png is a ripped PSP game asset ("Rip by Davias").
tools/phoenix/reference/

# Implementation plans are working notes, not project docs. Specs stay.
docs/superpowers/plans/

# Python bytecode from the sprite pipeline in tools/phoenix/scripts.
__pycache__/
*.pyc
